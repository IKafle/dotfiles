

#SWITCH TO TERMINAL SHORT CUTS
alias tone='sudo chvt 1'                                
alias ttwo='sudo chvt 2'
alias tthree='sudo chvt 3'
alias tfour='sudo chvt 4'
alias tfive='sudo chvt 5'
alias tsix='sudo chvt 6'
alias tseven='sudo chvt 7'

alias cls='clear && ls'

# SHORT CUTS TO DOT FILES.
alias en='sudo emacs -nw ~/.bin/dotFiles/bash/env'
alias fun='sudo emacs -nw ~/.bin/dotFiles/bash/function'
alias con='sudo emacs -nw ~/.bin/dotFiles/bash/config'
alias al='sudo emacs -nw ~/.bin/dotFiles/bash/aliases'
alias pr='sudo emacs -nw ~/.bin/dotFiles/bash/prompt'
alias lisnepal='cd ~/Desktop/vpn && sudo openvpn --config ishwor.kafle@pdc01.lisnepal.com.np__ssl_vpn_config.ovpn'


# BATTERY PERCENTAGE 
alias y='upower -i $(upower -e | grep BAT) | grep --color=never -E "state|to\ full|to\ empty|percentage"' 

# CHANGE DIRECTORIES SHORT CUTS
alias one='cd ..'
alias two='cd ../../'
alias three='cd ../../../'
alias four='cd ../../../../'
alias five='cd ../../../../../'

#alias parent="git show-branch | grep '*' | grep -v "$(git rev-parse --abbrev-ref HEAD)" | head -n1 | sed 's/.*\[\(#.*\)\].*/\1/' | sed 's/[\^~].*//'"
export NODE_OPTIONS="--max-old-space-size=5120"

# raizor docker container
alias up='docker/sdk up -x'
alias cli='docker/sdk cli -x'
alias dc='docker/sdk up -x && docker/sdk cli -x'
alias console='docker/sdk console'
alias generate='docker/sdk console transfer:generate'
alias propel='docker/sdk console propel:install'
alias clean='docker/sdk clean && rm -rf vendor && rm -rf src/Generated && docker/sdk boot deploy.dev.yml && docker/sdk up --build --assets --data'
alias runTests='docker/sdk console cache:class-resolver:build && docker/sdk testing -x codecept run -c codeception.acceptance.yml'
